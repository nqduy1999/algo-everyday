function isBalancedBrackets(str) {
  const matchingBracket = {
    "{": "}",
    "[": "]",
    "(": ")",
  };

  const expectedMatches = str.length / 2;
  const actualMatches = 0;

  let hashMap

  for (let i = 0; i < str.length; i++) {}
}

console.log(isBalancedBrackets(`([])`));
